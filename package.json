{
  "name": "dronewatch-static",
  "version": "2.0.0",
  "private": true,
  "description": "DroneWatch - European Drone Incident Tracker",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index-public.html dist/index.html && cp -r js dist/ && cp -r css dist/ 2>/dev/null || true && cp -r images dist/ 2>/dev/null || true && cp incidents.json dist/incidents.json && cp manifest.json dist/manifest.json 2>/dev/null || true",
    "vercel-build": "rm -rf dist && mkdir -p dist && cp index-public.html dist/index.html && cp -r js dist/ && cp -r css dist/ 2>/dev/null || true && cp -r images dist/ 2>/dev/null || true && cp incidents.json dist/incidents.json && cp manifest.json dist/manifest.json 2>/dev/null || true"
  }
}